#ifndef BOOT_CONFIG_H
#define BOOT_CONFIG_H

/* ====================================================================
 *  БЫСТРАЯ ЗАГРУЗКА (FAST_BOOT)
 *  --------------------------------------------------------------------
 *  1 = релизный режим: пропускаем искусственные паузы, тяжелую boot-
 *      диагностику и стресс-тесты ФС/PMM. Драйверы, не нужные для 
 *      первого кадра GUI (PCI/USB/звук/сеть), инициализируются в фоне.
 *  0 = отладочный режим: полный диагностический лог и все тесты.
 * ==================================================================== */
#ifndef FAST_BOOT
#define FAST_BOOT 1
#endif

#ifndef DEFER_HW_INIT
#define DEFER_HW_INIT FAST_BOOT
#endif

/* ====================================================================
 *  BSD/Linux-Style RAW CONSOLE MODE
 *  --------------------------------------------------------------------
 *  Мы полностью отключаем графические заставки ради чистой текстовой 
 *  консоли скроллинга ядра.
 * ==================================================================== */
#define BOOT_SCREEN_MODE   2    // 2 = Режим чистой текстовой консоли BSD/Linux
#define BOOT_PROGRESS_BAR  0    // Отключено ради консоли
#define BOOT_SHOW_NYAN     0    // Отключено
#define BOOT_SHOW_PROGRESS 0    // Отключено
#define BOOT_SHOW_SPINNER  0    // Отключено

#endif /* BOOT_CONFIG_H */